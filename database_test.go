package main

import "testing"

func TestDB(t *testing.T) {
	/*
	   db := NewDB()
	   bp := db.getBufferPool()

	   td := NewTupleDesc([]Type{INT_TYPE{}, INT_TYPE{}}, []string{"f1", "f2"})
	   tp := NewTuple(td)
	   tp.setField(0, NewIntField(111))
	   tp.setField(0, NewIntField(222))
	   bp.insertTuple(0, db.getCatalog().getTableID("hello_DB"), tp)
	*/
}
